# TODO
- [ ] Titlebar у PWA приложения выводит то чёрный, то белый. Нужно кешировать текущую тему и сразу красить titlebar нужным цветом (`theme-color`) при загрузке, чтобы не было мигания.
- [ ] Делать `trim()` при конвертации текста в JSON (text2json) — обрезать ведущие/замыкающие пробелы и переводы строк перед `JSON.stringify`.
