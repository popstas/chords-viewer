# TODO

> Стек: **Nuxt 3 + Vue 3 + TypeScript + Pinia**, Element Plus, Firebase v9 modular.
> Переезд с Nuxt 2 завершён (фазы 0–5 + пост-миграционные фиксы — в истории git).

- [x] **Закрыть npm audit (21 уязвимостей) мажорными апгрейдами.** Все фиксы требуют
  `npm audit fix --force` / breaking-мажоров: Firebase 9→12 (закрывает критическую
  protobufjs + high grpc-js/firestore), Nuxt 3.13→3.21+ (closes esbuild/nanotar/unhead/
  devtools XSS, vite-builder). Делать отдельной веткой с тестами Firebase auth/sync под
  логином и проверкой плеера/таймингов. Прочие мажоры (@vueuse/core 11→14, fuse.js 6→7,
  vue-virtual-scroller 2→3, eslint 8→10, fortawesome 6→7) — по желанию, не security.
- [x] Определять припевы в тексте песни, после припевов разделитель hr должен быть жирнее.
  Припев определять по одинаковым началам блоков (повторяющиеся строки) — значит остальные блоки
  куплеты; также учитывать явную пометку «Куплет», «Припев» перед блоком. На английском языке - "Chorus".
- [x] Добавь nowrap режим - когда строки аккордов не переносятся, а скроллятся вправо-влево. Если не входит строка аккордов, то нужно уменьшать пробелы между аккордами. Контрол: кнопка-иконка слева от beat-toggle-button. Не запоминает состояние, по умолчанию выключен. Показывается только когда какие-то строки аккордов не входят в ширину экрана.
- [ ] Офлайн-режим для shows: сохранять изменения статистики/просмотров (shows) локально, когда нет сети; при появлении онлайна сбрасывать (flush) накопленные изменения в Firebase и синхронизировать. При мердже для каждой песни предпочитать большее значение shows (prefer bigger shows for each song).
