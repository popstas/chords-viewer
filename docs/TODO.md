# TODO
- [ ] Интерфейс для конвертации текста в JSON. Desktop only. Ссылка в сайдбаре, открывает popup с двумя textarea и кнопкой "Convert": первая textarea — обычный текст, вторая — JSON-значение (обёрнутое в `""`, через `JSON.stringify`). Кнопка "Copy to clipboard" для результата.
