# TODO

## Очередь воспроизведения (плейлист)

- [ ] Добавить очередь песен. При добавлении песни в очередь создаётся временный жанр `next`
  (фильтр в списке жанров), показывающий песни из очереди. Кнопка «добавить в очередь» —
  слева от inline-контролов размера шрифта (в `SongItem`). У плейлиста есть кнопка очистки.

## UI

- [ ] На десктопе при открытии песни сдвигать текст на 1 строку ниже, чтобы floating chords
  (`ChordsFloating`, закреплённые сверху) не загораживали первую строку текста.

## Навигация по куплетам

- [ ] Определять куплеты в тексте песни и сделать переключение между ними. Припев определять
  по одинаковым началам блоков (повторяющиеся строки) — значит остальные блоки куплеты; также
  учитывать явную пометку «Куплет» перед блоком. В правом нижнем углу — большая (75×75)
  полупрозрачная кнопка для промотки к следующему куплету.

## Закончить переезд на Vue 3 (ветка `v5`) — отложено, после MIDI-плеера

> Делаем после переписывания MIDI-плеера. Плеер дорабатываем на `master`; затем при переезде
> на Vue 3 портируем уже новую версию плеера.

Ветка `v5` (есть локально и `origin/v5`) — миграция Nuxt 2 → **Nuxt 3 + Vue 3 + TypeScript +
Vuex 4**. Состояние WIP: «app builded, but doesn't start», отстаёт от `master` на 7 коммитов
(в т.ч. перенос chord maps в utils и AGENTS). PR пока нет — нужно довести до рабочего состояния
и открыть PR. Самое сложное — ленивая подгрузка списка песен на мобильной версии.

- [ ] **Синхронизировать `v5` с `master`.** Влить актуальный `master` в `v5` (7 коммитов:
  перенос chord maps в utils, AGENTS, фиксы тем/иконок и др.), разрешить конфликты, чтобы база
  миграции была свежей.
- [ ] **Поднять приложение на Nuxt 3.** Добиться, чтобы `npm run dev` стартовал и приложение
  открывалось (сейчас билдится, но не запускается). Починить рантайм-ошибки переезда: Vuex 4
  store, Element UI → совместимый с Vue 3 UI-кит, плагины (`vue-shortkey`, `vue-qrcode-reader`,
  `vue-virtual-scroller@2`), Firebase auth/sync, WebAudioFont/MIDI плеер.
- [ ] **Ленивая подгрузка списка песен на мобиле (главная сложность).** Реализовать ленивую
  загрузку/виртуализацию списка песен для мобильной версии (`screen.width <= 600`), чтобы длинный
  список не грузил всё сразу и не тормозил. Сохранить мобильный автоскролл и поведение панелей.
- [ ] **Проверка паритета функций.** Сверить, что после переезда работают все ключевые функции:
  поиск/фильтры, транспонирование, MIDI/beat-плеер, floating-панели (`ChordsFloating`,
  `PlayerFloating`, `Toolbar`), аккорды по ховеру, авторизация и синк статов через Firebase,
  параметры из URL. Затем открыть PR из `v5` в `master`.

