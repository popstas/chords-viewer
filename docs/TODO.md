# TODO

## Переписать MIDI-плеер (`components/BeatPlayer.vue`)

Цель — надёжное воспроизведение без заиканий (сейчас сбивается ~раз в минуту, тормозит
на телефоне, чувствителен к задержке ~50 мс) и переключение инструментов на границе тактов.
Все функции приложения сохранить: стили пиано, пресеты, октава, sustain, rever, drums-only /
piano-only / piano-in-drums, кастомные инструменты, BPM-слайдер/метр, горячие клавиши, чтение
параметров из URL, проброс `beatProgress`/`beatPlaying` в store для `ChordsFloating`.

- [ ] **Планировщик на look-ahead вместо RAF.** Заменить цикл `tick()` на
  `requestAnimationFrame` планировщиком по аудио-часам (`AudioContext.currentTime`) на
  `setTimeout` с окном упреждения ~100–150 мс и шагом ~25 мс (паттерн «A Tale of Two Clocks»).
  Это убирает зависимость от частоты кадров и переживает подвисания UI до окна упреждения.
  Сохранить текущую логику петли/синка двух треков (`songDrums` + `songPiano`), интро-отсчёт,
  смещение под `outputLatency`.

- [ ] **Эффективная очередь нот.** Не сканировать все ноты трека на каждом шаге
  (сейчас `sendNotes` — O(n) по всем нотам каждый тик). Держать индекс/указатель на следующую
  ноту и ставить в очередь только попадающие в окно упреждения; для петли — индекс с переносом
  через границу цикла. Убрать deep-clone через `JSON.parse(JSON.stringify())` из `applyBpm`
  в горячем пути (минимизировать аллокации/GC ради телефона).

- [ ] **Переключение инструмента на границе 4 тактов.** Сейчас смена `pianoInstrument`
  (radio, пресеты, кастомный инструмент) применяется «на лету» в `sendNotes` и бывает с большой
  задержкой. Предзагружать выбранный soundfont заранее и применять смену на ближайшей границе
  цикла (`beatCycleDuration` = 4 такта), чтобы переход был бесшовным и в ритм.

- [ ] **Развязать прогресс/UI от планировщика.** Обновление `beatProgress` (и коммит в store
  для метронома `ChordsFloating`) вынести в отдельный RAF только для отображения, чтобы
  перерисовка прогресс-бара не влияла на тайминг звука. Поведение метронома на мобиле сохранить.

- [ ] **Live-изменения без рестарта.** BPM (debounce), `chordBeats`, `pianoStyle`, `rever`,
  октава/sustain применять перепланированием на границе цикла вместо жёсткого `replay()` с
  перезапуском с нуля, где это возможно. Не ломать кейсы drums-only / piano-only.

- [ ] **Проверка регрессий и производительности.** Прогнать чек-лист: все стили пиано, 2/4/8
  beats, пресеты (добавить/очистить/переключить), октавы, sustain, rever, drums-only,
  piano-only, piano-in-drums (`highestPitch >= 60`), кастомные инструменты, BPM-слайдер,
  горячие клавиши (b/c/d и рус. раскладка), параметры из URL, синк метронома в `ChordsFloating`.
  Проверить отсутствие заиканий на телефоне в длинном проигрывании (несколько минут).
