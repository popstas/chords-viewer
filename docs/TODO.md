# TODO

- [ ] Переписать / оптимизировать MIDI-плеер (`components/BeatPlayer.vue`), сохранив все функции приложения. Текущий плеер написан на низкоуровневых JS-циклах, заикается примерно раз в минуту (даже задержка ~50 мс сбивает ритм), тормозит на телефоне. Нужен надёжный планировщик на Web Audio (look-ahead scheduling по `AudioContext.currentTime` вместо `setTimeout`/циклов), без jitter. Дополнительно: переключение инструментов сейчас происходит с большой задержкой — нужно, чтобы смена инструмента применялась на границе 4 тактов.
